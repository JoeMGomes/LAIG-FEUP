/*
    keyframe class
*/


class KeyFrame {

    constructor(instant, transform, scale, rotate) {
        this.instant = instant;
        this.transform = transform;
        this.scale = scale;
        this.rotate = rotate;

    }

}